/**
 * Planning — Hanzo App built-in skill
 * Starting a project: the PLAN.md workflow, the stack a Hanzo project already has, and the file layout.
 */

export const PLANNING_SKILL: string = "---\nname: planning\ndescription: Read when starting a new project. Covers the PLAN.md workflow, the stack a Hanzo project already has, and the file layout.\n---\n\n# Planning a project\n\n## Write `/PLAN.md` first\n\n```markdown\n# Project plan\n\n## Overview\nWhat this site is and who opens it.\n\n## Pages\n- index.html — hero, what it does, one call to action\n- about.html — the story\n- contact.html — a form that writes to Hanzo Base\n\n## Data\n- collection `signups`: name, email        (a form persists, or it does not exist)\n- collection `messages`: name, body\n\n## Components\n- /templates/components/nav.hbs\n- /templates/components/footer.hbs\n\n## Order\n1. Components\n2. index.html, finished\n3. Remaining pages\n4. Mobile menu, motion, empty states\n```\n\nThen build it in that order. The plan is what keeps a session from producing\neight half-pages.\n\n## The stack is already decided\n\nNothing here is a choice to be made per project, and there is no second option\nfor any row. Everything is served from `https://hanzo.app/vendor/` — one origin,\npinned versions, no third party watching your visitors.\n\n| Concern | What you use |\n|---|---|\n| Style | `<link rel=\"stylesheet\" href=\"https://hanzo.app/vendor/design/styles.css\"/>` — the Hanzo design system: tokens plus a base layer that styles bare HTML. Plain CSS on top. No utility framework. |\n| Type | Geist sans and mono, inside that stylesheet. No third face, no font CDN. |\n| Icons | `<script src=\"https://hanzo.app/vendor/feather.js\"></script>`, then `feather.replace()`. |\n| Motion | CSS transitions and the sheet's own keyframes (`hanzo-fade-up`, `hanzo-slide-up-fade`). No animation library, no scroll-reveal. |\n| Maps | `https://hanzo.app/vendor/leaflet.js` + `leaflet.css`, OpenStreetMap tiles. Needs no key. |\n| Images | `http://static.photos/<category>/<dimensions>/<seed>` — always pass a seed. |\n| Data, forms, auth | Hanzo Base. `fetch('/v1/base/<collection>')` to read, POST to write, `/v1/me` for who is signed in. |\n\nDo not reach for Tailwind, Bootstrap, Bulma, FontAwesome, Google Fonts, AOS,\nNetlify Forms, Formspree or Basin. The origins they live on are refused by the\npolicy a generated page runs under, so a page that names one loads nothing at\nall — and every one of them is already covered by a row above.\n\n## Colour is not a decision either\n\nHanzo is monochrome: true black ground, white type, and colour **only** as\nstate — `--state-error` for a blocking error, `--state-online` for a live dot,\n`--state-success` for a positive callout. There is no primary/secondary/accent\npalette to pick, because there is no decorative colour. A page distinguishes\nitself with type, space and content, not hue. See the `design-system` skill for\nthe token vocabulary.\n\n## Layout\n\n```\n/\n├── PLAN.md\n├── index.html\n├── about.html\n├── contact.html\n└── templates/\n    └── components/\n        ├── nav.hbs\n        └── footer.hbs\n```\n\nEach page is self-contained: its CSS in one `<style>` in the head, its\nJavaScript in an inline `<script>`. Reference a separate `.css` or `.js` file\nonly when you are also creating that file — a `<script src>` that 404s takes\nevery reference to it down with the page.\n\n## Do, don't\n\n**Do** plan every page before writing one · plan the collections a form needs ·\ndesign at 390px first · reuse `nav` and `footer` from the start · name a token\nrather than a value.\n\n**Don't** skip PLAN.md · mix in a second UI kit · plan a server, a database of\nyour own, or a login form — Hanzo Base is the backend and Hanzo is the identity ·\ninvent metrics, testimonials or prices · over-scope: finish four pages rather\nthan start nine.";
