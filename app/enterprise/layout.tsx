import type { Metadata } from "next";
export { default } from "@/lib/seo";

export const metadata: Metadata = {
  title: "Enterprise",
  description:
    "Hanzo for organizations: one org with many seats, single sign-on, role-based access, an audit trail, and a named person to call.",
  alternates: { canonical: "/enterprise" },
};
