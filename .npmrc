# Force usage of pnpm
engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# withGui writes .gui/*.mjs bundles whose externals are bare @hanzogui/* imports;
# hoist the family so those resolve from the app root under strict pnpm layout
public-hoist-pattern[]=@hanzogui/*
