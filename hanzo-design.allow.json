{
  "_": "What this surface still owes the design system, per hanzo-design-lint rule. It may only SHRINK \u2014 a fix deletes its own exemption in the same commit, and a violation written today fails the build. `pnpm lint:design` is the gate; @hanzo/design's LLM.md documents the four layers (values\u2192design, structure\u2192gui, chrome\u2192shell, components\u2192ui) and every rule.",
  "_zero": "local-token-table, second-publisher, raw-font-size, icon-set and wrong-package are absent, which means ZERO. The two that mattered were closed by the paper commit: there is no local createGui table, and @hanzo/brand no longer publishes over @hanzo/design.",
  "_backlog": "raw-color is overwhelmingly the generated-site skills and prompt decks \u2014 copy that TEACHES a model to write CSS, which is data and not chrome \u2014 plus the code editor's own syntax palette. unresolved-token names custom properties nothing declares (the landing's `caret`/`rise`/`wt-*` family is the visible half). shell-owned-chrome is four local surfaces that should compose @hanzogui/shell instead.",
  "unresolved-token": 96,
  "raw-color": 1181,
  "raw-z-index": 66,
  "all-caps": 1,
  "inline-style": 6,
  "redeclared-token": 10,
  "shell-owned-chrome": 3
}
