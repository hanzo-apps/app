diff --git a/dist/gui-config.cjs b/dist/gui-config.cjs
index 7b2550eab227bfebfd6699fc2cb27f3b83a635dc..7912c6134cc7d3fb17a47ef9f1099eab0e9fe602 100644
--- a/dist/gui-config.cjs
+++ b/dist/gui-config.cjs
@@ -156,11 +156,11 @@ const STEP = {
 };
 const space = {};
 for (const [k, v] of Object.entries(STEP)) {
-    space[`$${k}`] = v;
-    space[`-${k}`] = -v;
+    space[`$${k}`] = `calc(${v}px * var(--density, 1))`;
+    space[`-${k}`] = `calc(${-v}px * var(--density, 1))`;
 }
-space.$true = STEP['4'];
-space['-true'] = -STEP['4'];
+space.$true = `calc(${STEP['4']}px * var(--density, 1))`;
+space['-true'] = `calc(${-STEP['4']}px * var(--density, 1))`;
 /**
  * THREE RUNGS OF THE NUMBERED RAMP UNDID A TOKEN DECISION, so they are re-based.
  *
diff --git a/dist/gui-config.js b/dist/gui-config.js
index b2ad1652dcb6b3879fde40b692cdbebf81909d56..ba337d50be0b61df01b5147294b183aadf15444b 100644
--- a/dist/gui-config.js
+++ b/dist/gui-config.js
@@ -153,11 +153,11 @@ const STEP = {
 };
 const space = {};
 for (const [k, v] of Object.entries(STEP)) {
-    space[`$${k}`] = v;
-    space[`-${k}`] = -v;
+    space[`$${k}`] = `calc(${v}px * var(--density, 1))`;
+    space[`-${k}`] = `calc(${-v}px * var(--density, 1))`;
 }
-space.$true = STEP['4'];
-space['-true'] = -STEP['4'];
+space.$true = `calc(${STEP['4']}px * var(--density, 1))`;
+space['-true'] = `calc(${-STEP['4']}px * var(--density, 1))`;
 /**
  * THREE RUNGS OF THE NUMBERED RAMP UNDID A TOKEN DECISION, so they are re-based.
  *
